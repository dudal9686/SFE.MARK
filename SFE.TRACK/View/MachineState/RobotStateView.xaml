<Window x:Class="SFE.TRACK.View.MachineState.RobotStateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SFE.TRACK.View.MachineState"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        x:Name="RobotStateModal"
        DataContext="{Binding RobotStateViewModel, Source={StaticResource Locator}}"
        Title="RobotState" Height="190" Width="200">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="10 5 10 5">
            <Button Height="50" FontSize="30" Command="{Binding RunRelayCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Play" Foreground="Green" Width="30" Height="30" Margin="-30 0 0 0"/>
                    <TextBlock Margin="20 0 0 0" FontSize="20">Run</TextBlock>
                </StackPanel>
            </Button>
            <Button Height="50" Margin="0 3 0 0" Command="{Binding StopRelayCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Stop" Foreground="SandyBrown" Width="30" Height="30" Margin="-30 0 0 0"/>
                    <TextBlock Margin="20 0 0 0" FontSize="20">Stop</TextBlock>
                </StackPanel>
            </Button>
            <Button Height="30" Margin="0 30 0 0" Command="{Binding CloseRelayCommand}" CommandParameter="{Binding ElementName=RobotStateModal}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Close" Foreground="Red" Width="30" Height="30" Margin="-30 -7 0 0"/>
                    <TextBlock Margin="20 -7 0 0" FontSize="20">Close</TextBlock>
                </StackPanel>
            </Button>

        </StackPanel>
    </Grid>
</Window>