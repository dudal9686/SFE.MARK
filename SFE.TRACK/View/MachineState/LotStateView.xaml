<Window x:Class="SFE.TRACK.View.MachineState.LotStateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SFE.TRACK.View.MachineState"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        x:Name="LotStateModal"
        DataContext="{Binding LotStateViewModel, Source={StaticResource Locator}}"
        Title="LotState" Height="190" Width="200">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="10 5 10 5">
            <Button Height="50" FontSize="30" Command="{Binding LotCreateRelayCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="CreditCard" Foreground="Blue" Width="30" Height="30" Margin="-30 0 0 0"/>
                    <TextBlock Margin="20 0 0 0" FontSize="20">Create</TextBlock>
                </StackPanel>
            </Button>
            <Button Height="50" Margin="0 3 0 0" Command="{Binding LotClearRelayCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Eraser" Foreground="SandyBrown" Width="30" Height="30" Margin="-35 0 0 0"/>
                    <TextBlock Margin="20 0 0 0" FontSize="20">Clear</TextBlock>
                </StackPanel>
            </Button>
            <Button Height="30" Margin="0 30 0 0" Command="{Binding CloseRelayCommand}" CommandParameter="{Binding ElementName=LotStateModal}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Close" Foreground="Red" Width="30" Height="30" Margin="-30 -7 0 0"/>
                    <TextBlock Margin="20 -7 0 0" FontSize="20">Close</TextBlock>
                </StackPanel>
            </Button>

        </StackPanel>
    </Grid>
</Window>
